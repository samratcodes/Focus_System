# Where the Node.js backend (../backend) runs. /api/* on this site is forwarded there.
BACKEND_URL="http://localhost:4000"
# Shown on the Settings page as the server address for the mobile app.
NEXT_PUBLIC_API_URL="http://localhost:4000"
